/*Объявить тип-структуру humen, включающую имя человека, фамилию и год рождения. 
Объявить два массива из четырёх элементов типа humen. 
Ввести с консоли или из фай-ла элементы одного массива и построить на их основе другой, упорядочив элементы по годам рождения. 
Вывести результат.*/

#include <stdio.h>

struct humen
{
    char name[10], surname[15]; //structure atributes 
    int birth_year;
};
    

void sort(struct humen **q, int n)
{
    int i, j;
    for (i = 0; i < n; i++)
    {
        for (j = 0; j < n - i - 1; j++)
        {
            // сравниваем элементы массива структур по дате рождения человека
            if (q[j]->birth_year > q[j+1]->birth_year)
            {
                struct humen *temp = q[j];
                q[j] = q[j+1];
                q[j+1] = temp;
            }
        }
    }
}


int main ()
{
    int n = 4, i;
    struct humen *p = struct newhumen[n];
    for (i = 0; i < n; i++)
    {
        printf("Enter the name, surname and birth_year: ");
        scanf("%s %s %d", &p[i] -> name, &p[i] -> surname, &p[i] -> birth_year);
        // printf("Name: %s\t Surname: %s\t Birth_year: %d\n", p[i].name, p[i].surname, p[i].birth_year);
    }
    struct humen **q = struct newhumen*[n];
    for (i = 0; i < n; i++)
    {
        q[i] = &p[i];
        
    }
    for (i = 0; i < n; i++)
    {
        q[i] = &p[i];
        printf("Name: %s\t Surname: %s\t Birth_year: %d\n", q[i] -> name, q[i] -> surname, q[i] -> birth_year);
    }
return 0;
}
