/*ЗАДАНИЕ 3
Ввести с консоли строку символов произвольной длины (до 80 символов). Поменять в ней все маленькие символы ‘a’ и ‘b’ на заглавные и вывести на консоль полученную строку.*/

#include <stdio.h>
#include <stdlib.h>
#include <string.h>

int main() 
{
    char row[80];
    fgets(row, 80, stdin);
    int len = strlen(row);
    for (int i = 0; i < len; i++) {
        if (row[i] == 'a' || row[i] == 'b') {
            row[i] -= 32;
        } else if (row[i] == 'A' || row[i] == 'B') {
            row[i] += 32;
        }
}

printf("%s", row);
return 0;
}
