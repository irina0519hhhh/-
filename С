#include <stdio.h>
int main()
{   int c, n, res;
    printf("Enter your value: "); 
    scanf("%i %i", &c, &n);
if (c >= n){ 
    res = c / n;
    printf("res = %i", res);
}else{
printf("cannot solve the ex!");
return 0;
}
}

