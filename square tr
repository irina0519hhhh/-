#include <stdio.h>
#include <math.h>

    double s; 
    double sq_tr (per_sq, a, b, c)
    if (s1 < 0 || s2 < 0 || s3 < 0)
    {
        return 0;
    }
    p = per_sq/2
    s = sqrt(p*(p-a)*(p-b)*(p-c))
    return s
