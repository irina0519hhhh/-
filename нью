for (int i = 0; i < 2; i++) { //читаем две строки (для двух матриц)
    char line[100];
    fgets(line, 100, f1); //читаем строку
    for (int j = 0; j < n; j++) {
        for (int k = 0; k < m; k++) {
            fscanf(f1, "%d", &mass1[j][k]); //читаем элементы первой матрицы
        }
    }
}
